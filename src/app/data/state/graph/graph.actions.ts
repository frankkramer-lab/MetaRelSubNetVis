import { createAction } from '@ngrx/store';

export const renderingSuccess = createAction('[Graph Effects] rendering success');
export const renderingFailure = createAction('[Graph Effects] rendering failure');
export const markingNodesSuccess = createAction('[Graph Effects] marking nodes success');
